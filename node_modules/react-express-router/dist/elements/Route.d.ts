import { ComponentsProps, Elements } from "../types";
import INode from "./Node";
declare class RouteInstance extends INode<Elements.Route> {
    constructor(type: any, props: any);
    render(): ComponentsProps;
}
export default RouteInstance;
