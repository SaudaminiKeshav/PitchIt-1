"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const types_1 = require("../types");
const Node_1 = __importDefault(require("./Node"));
const utils_1 = require("../utils");
const errorHandler_1 = require("../validation/errorHandler");
class RouteInstance extends Node_1.default {
    constructor(type, props) {
        super(type, props);
        this.props = Object.assign(Object.assign({}, props), { path: props && props.path ? props && props.path : utils_1.DEFAULT_PATH });
    }
    render() {
        const { path, handle, method, strict, caseSensitive, mergeParams, } = this.props;
        const routes = this.childs.map(c => c.render());
        errorHandler_1.noErrorHandler(routes);
        return {
            type: types_1.Elements.Route,
            path,
            handle,
            method,
            routes,
            strict,
            caseSensitive,
            mergeParams,
        };
    }
}
exports.default = RouteInstance;
