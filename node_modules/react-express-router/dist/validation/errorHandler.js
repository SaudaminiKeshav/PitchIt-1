"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const types_1 = require("../types");
function noErrorHandler(routes) {
    const errorHandlers = routes.filter(n => n.type === types_1.Elements.ErrorHandler);
    if (errorHandlers.length > 0) {
        throw new Error('Error there are error handler inside the route component. Must be inside Express.');
    }
}
exports.noErrorHandler = noErrorHandler;
function twiceErrorHandler(routes) {
    const errorHandlers = routes.filter(n => n.type === types_1.Elements.ErrorHandler);
    if (errorHandlers.length > 1) {
        throw new Error('Error there are twice error handler.');
    }
}
exports.twiceErrorHandler = twiceErrorHandler;
